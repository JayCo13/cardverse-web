// MARKETPLACE LOCKED — Pivot to Portfolio Tracker + Scanner
// To restore: revert this file from git (git checkout -- src/app/buy/page.tsx)
import { redirect } from 'next/navigation';
export default function BuyPageLocked() {
  redirect('/');
}
